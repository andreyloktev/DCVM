
set(projectName
    DCVM
)

set(components
    eastl
    dcvm
    dcvm_provider
    dcvm_google
    filemanager_cli
)

if(WIN32)
    list(APPEND components
        dcvm_windows
    )
endif()